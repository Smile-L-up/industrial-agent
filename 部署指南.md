# 工业智能体 Docker 部署指南（极简版）

## 更新说明

### v3.0 极简版更新

**已移除的依赖**：
- `numpy` - 原用于 RAG 模块，RAG 已移除
- `aiohttp` - 改用 `httpx` 替代
- `asyncio` - Python 3.11 内置模块，无需安装
- `pyyaml` - 代码中未使用
- `toml` - 代码中未使用
- `pillow` - 图片处理使用 httpx 即可
- `mcp` - 代码中未使用
- `pytest, black, flake8, mypy` - 开发工具，运行时不需要

**镜像优化**：
- 使用稳定的 `python:3.11-slim` 基础镜像
- 设置 `PIP_NO_CACHE_DIR=1` 减少镜像大小
- 只复制必要的项目文件

**镜像大小对比**：

| 版本 | 基础镜像 | 镜像大小 |
|------|----------|----------|
| v1.0 | python:3.11-slim | ~5-6GB |
| v2.0 | python:3.11-slim | ~500MB-1GB |
| v3.0 | python:3.11-slim | ~400-600MB |

---

## 快速部署

### 第一步：构建镜像

```bash
docker build -t industrial-agent:latest .
```

### 第二步：导出镜像

```bash
docker save -o industrial-agent.tar industrial-agent:latest
```

### 第三步：传输到内网

将 `industrial-agent.tar` 文件复制到内网机器。

### 第四步：导入并启动

```bash
# 导入镜像
docker load -i industrial-agent.tar

# 启动容器
docker run -d \
  --name industrial-agent \
  -p 18000:18000 \
  -e DASHSCOPE_API_KEY=your_api_key \
  -e LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1 \
  industrial-agent:latest
```

---

## 使用 docker-compose 部署

### 启动服务

```bash
docker compose -f docker-compose.intranet.yml up -d
```

### 查看状态

```bash
docker compose ps
docker compose logs -f
```

### 停止服务

```bash
docker compose down
```

---

## 环境变量配置

### .env 文件配置

```bash
# LLM API 密钥
DASHSCOPE_API_KEY=your_api_key

# LLM 配置
LLM_PROVIDER=openai
LLM_MODEL=qwen3.5-plus
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 系统配置
TZ=Asia/Shanghai
LOG_LEVEL=INFO
```

---

## 验证部署

```bash
# 查看容器状态
docker ps

# 查看日志
docker logs -f industrial-agent

# 测试 API
curl http://localhost:18000/health
curl http://localhost:18000/api
```

---

## 代码更新（无需重新构建镜像）

### 方式一：使用 docker cp 命令

```bash
# 更新单个文件
docker cp config.py industrial-agent:/app/config.py

# 更新整个目录
docker cp agent/ industrial-agent:/app/agent/

# 重启容器使更改生效
docker restart industrial-agent
```

### 方式二：使用 docker-compose 挂载代码

1. 编辑 `docker-compose.intranet.yml`，取消代码挂载注释：

```yaml
volumes:
  - ./logs:/app/logs
  - ./data:/app/data
  - ./agent:/app/agent
  - ./core:/app/core
  - ./llm:/app/llm
  - ./skills:/app/skills
  - ./prompts:/app/prompts
  - ./static:/app/static
  - ./api.py:/app/api.py
  - ./config.py:/app/config.py
  - ./main.py:/app/main.py
```

2. 重启服务：
```bash
docker compose restart
```

---

## 常见问题

### 1. 镜像大小

**当前镜像大小**：约 300-500MB

**包含内容**：
- Python 3.11 运行时
- 项目依赖包（pydantic, openai, requests, httpx, fastapi, uvicorn, loguru 等）
- 项目代码

**已移除**：
- ~~torch（约 2GB）~~
- ~~transformers（约 500MB）~~
- ~~sentence-transformers~~
- ~~numpy~~
- ~~aiohttp~~
- ~~RAG 模块~~
- ~~开发工具（pytest, black, flake8, mypy）~~

### 2. 如何只更新代码而不重新构建镜像？

**答**：有两种方式：

**方式一**：使用 `docker cp` 命令
```bash
docker cp 本地文件路径 industrial-agent:/app/文件路径
docker restart industrial-agent
```

**方式二**：使用 docker-compose 挂载代码卷
在 `docker-compose.intranet.yml` 中配置 volumes，将代码目录挂载到容器中。

### 3. 更新代码后需要重新构建吗？

**答**：
- 如果只修改 Python 代码（.py 文件）→ **不需要**重新构建
- 如果修改了 `requirements.txt` 需要添加新依赖 → **需要**重新构建
- 如果修改了 Dockerfile → **需要**重新构建

---

## 快速参考

### 外网机器命令

```bash
# 构建镜像
docker build -t industrial-agent:latest .

# 导出镜像
docker save -o industrial-agent.tar industrial-agent:latest
```

### 内网机器命令

```bash
# 导入镜像
docker load -i industrial-agent.tar

# 启动容器
docker run -d --name industrial-agent -p 18000:18000 --env-file .env industrial-agent:latest

# 使用 docker-compose
docker compose -f docker-compose.intranet.yml up -d

# 更新代码
docker cp config.py industrial-agent:/app/config.py
docker restart industrial-agent
```

---

## 文件清单

### 第一次部署需要的文件

```
industrial-agent/
├── Dockerfile
├── docker-compose.intranet.yml
├── requirements.txt
├── .env.example
├── .dockerignore
├── agent/
├── core/
├── llm/
├── skills/
├── prompts/
├── static/
└── *.py
```

### 后续更新只需要

```
需要更新的文件
├── agent/
├── core/
├── llm/
├── skills/
├── prompts/
├── static/
├── api.py
├── config.py
└── main.py
```

---

## 服务地址

部署完成后，可通过以下地址访问服务：

- **API 地址**: http://内网机器 IP:18000
- **健康检查**: http://内网机器 IP:18000/health
- **前端页面**: http://内网机器 IP:18000/static/index.html

---

## 依赖说明

### 当前依赖列表

| 依赖 | 用途 |
|------|------|
| pydantic | 数据验证 |
| openai | LLM 客户端 |
| requests | HTTP 客户端 |
| httpx | 异步 HTTP 客户端（用于图片下载） |
| python-dotenv | 环境变量加载 |
| fastapi | Web 框架 |
| uvicorn | ASGI 服务器 |
| sse-starlette | SSE 流式响应支持 |
| loguru | 日志库 |

### 已移除的依赖

| 依赖 | 移除原因 |
|------|----------|
| numpy | RAG 模块已移除 |
| aiohttp | 改用 httpx |
| asyncio | Python 内置模块 |
| pyyaml | 代码中未使用 |
| toml | 代码中未使用 |
| pillow | 图片处理不需要 |
| mcp | 代码中未使用 |
| pytest/black/flake8/mypy | 开发工具，运行时不需要 |