# Industrial Agent Requirements
# 智能体项目依赖（极简版）

# ==================== 核心依赖 ====================
pydantic>=2.0.0

# ==================== LLM 客户端 ====================
# OpenAI 及兼容 OpenAI 接口的服务（如 Qwen/DashScope）
openai>=1.0.0

# ==================== HTTP 客户端 ====================
# 用于图片处理和 HTTP 技能
httpx>=0.25.0
pillow>=10.0.0

# ==================== 环境变量 ====================
python-dotenv>=1.0.0

# ==================== Web 服务 ====================
fastapi>=0.104.0
uvicorn>=0.24.0
sse-starlette>=1.8.0
python-multipart>=0.0.6

# ==================== 数据库 ====================
# SQLite 是 Python 内置，无需额外安装
# MySQL 支持（可选，当使用 MySQL 时需要）
pymysql>=1.1.0

# ==================== 已移除的依赖说明 ====================
# numpy - 原用于 RAG 模块，已移除
# aiohttp - 改用 httpx 替代
# asyncio - Python 3.11 内置模块，无需安装
# pyyaml - 未使用
# toml - 未使用
# pillow - 图片处理使用 httpx 即可
# mcp - 未使用
# pytest, black, flake8, mypy - 开发工具，运行时不需要
